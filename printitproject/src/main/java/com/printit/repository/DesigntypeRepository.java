package com.printit.repository;

import com.printit.model.Dtype;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DesigntypeRepository extends JpaRepository<Dtype, Integer> {
}
